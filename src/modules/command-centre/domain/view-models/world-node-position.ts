/**
 * Position on the world map canvas, expressed as percentages (0-100).
 */
export interface WorldNodePosition {
  readonly x: number; // 0-100 percentage
  readonly y: number; // 0-100 percentage
}
